server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seckillms?characterEncoding=utf8mb4&&serverTimeZone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
  redis:
    port: 6379
    host: localhost
  jackson:
    # 日期格式
    date-format: yyyy-MM-dd